package WeekCompetition.Week_282;import java.util.Arrays;import java.util.Comparator;public class MinimumFinishTime {//    public int minimumFinishTime(int[][] tires, int changeTime, int numLaps) {//        final var inf = (int) 2e8;//        var n = tires.length;//        var minSec = new int[28];//        Arrays.fill(minSec, inf);//        var sumSec = new int[n];//        for (var x = 1; x <= 27; ++x)//            for (var i = 0; i < n; ++i) {//                sumSec[i] = Math.min(sumSec[i] + tires[i][0], inf); // 累加该轮胎连续使用所消耗的时间//                minSec[x] = Math.min(minSec[x], sumSec[i]);//                tires[i][0] = (int) Math.min((long) tires[i][0] * tires[i][1], inf);//            }////        var f = new int[numLaps + 1];//        Arrays.fill(f, inf);//        f[0] = -changeTime;//        for (var i = 1; i <= numLaps; ++i) {//            for (var j = 1; j <= Math.min(27, i); ++j)//                f[i] = Math.min(f[i], f[i - j] + minSec[j]);//            f[i] += changeTime;//        }//        return f[numLaps];//    }}